<%= form.select(
  :app_id,
  (
    options_for_select([blank_option], selected_value) +
    grouped_options_for_select(options, selected_value)
  ),
  {},
  {
    class: 'form-select',
    data: { 'controller' => 'app-select-component' }
  }
) %>
