<%= form.select(
  :game_id,
  (
    options_for_select([blank_option], selected_value) +
    options_for_select(options, selected_value)
  ),
  {},
  {
    class: 'form-select',
    data: { 'controller' => 'game-select-component' }
  }
) %>
