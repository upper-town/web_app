<h2>
  <%= t("users.change_email_reversions.edit.title") %>
</h2>

<%= form_with(
  model: @change_email_reversion,
  method: :put,
  url: users_change_email_reversion_path
) do |form| %>
  <%= form.hidden_field(:token) %>

  <div class="my-3">
    <%= form.label(:code, class: "form-label") %>
    <%= form.text_field(
      :code,
      autofocus: true,
      autocomplete: "off",
      class: "form-control",
      aria: { describedby: "code-help" },
      required: true
    ) %>
  </div>

  <div id="code-help" class="form-text">
    <%= t("users.change_email_reversions.edit.code_help") %>
  </div>

  <div class="my-3">
    <%= form.submit(
      t("users.change_email_reversions.edit.submit"),
      class: "btn btn-primary",
      data: {
        "turbo-submits-with" => t("shared.button.confirming")
      }.compact,
    ) %>
  </div>
<% end %>
