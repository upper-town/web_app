<h2>
  Change Email: reversion
</h2>

<%= form_with(
  model: @change_email_reversion,
  method: :put,
  url: users_change_email_reversion_path,
  data: { 'controller' => 'form-error-messages-handler' }
) do |form| %>
  <%= form.hidden_field(:token) %>

  <div class="my-3">
    <%= form.submit(
      'Confirm and restore my email address',
      class: 'btn btn-primary',
      data: {
        'turbo-submits-with' => 'Confirming...',
        'controller' => @change_email_reversion.auto_click ? 'auto-click' : nil
      }.compact,
    ) %>
  </div>
<% end %>
