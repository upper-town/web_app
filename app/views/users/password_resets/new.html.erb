<% content_for(:head) do %>
  <%= captcha_script_tag %>
<% end %>

<h2>
  <%= t("users.password_resets.new.title") %>
</h2>

<%= form_with(
  model: @password_reset,
  method: :post,
  url: users_password_reset_path,
  data: { "controller" => "form-error-messages-handler" }
) do |form| %>
  <div class="mb-3">
    <%= form.label(:email, class: "form-label") %>
    <%= form.email_field(
      :email,
      autofocus: true,
      autocomplete: "email",
      class: "form-control",
      aria: { describedby: "email-help" },
      required: true
    ) %>
    <div id="email-help" class="form-text">
      <%= t("users.password_resets.new.email_help_html") %>
    </div>
  </div>

  <div class="mb-3">
    <%= captcha_widget_tag %>
  </div>

  <div class="mb-3">
    <%= form.submit(
      t("users.password_resets.new.submit"),
      class: "btn btn-primary",
      data: { "turbo-submits-with" => t("shared.button.loading") }
    ) %>
  </div>
<% end %>
