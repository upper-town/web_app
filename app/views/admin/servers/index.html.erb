<%= render(partial: 'shared/pagination', object: PaginationPresenter.new(@pagination)) %>

<% if @servers.empty? %>
  No results
<% else %>
  <%=
    # TODO: Fix this
    render(Admin::TablePresenter.new(
      collection: @servers,
      columns: [
        ['ID',      :id],
        ['Actions', proc { |server| render(partial: 'edit_actions_index', locals: { server: server }) }],
      ],
    ))
  %>
<% end %>

<%= render(partial: 'shared/pagination', object: PaginationPresenter.new(@pagination)) %>
