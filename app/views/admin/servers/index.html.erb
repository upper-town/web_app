<% if @servers.empty? %>
  No results
<% else %>
  <%= render(PaginationComponent.new(pagination: @pagination)) %>

  <%=
    render(Admin::TableComponent.new(
      collection: @servers,
      columns: [
        ['ID',      :id],
        ['SUUID',   proc { |server| server.suuid }],
        ['Actions', proc { |server| render(partial: 'edit_actions_index', locals: { server: server }) }],
      ],
    ))
  %>

  <%= render(PaginationComponent.new(pagination: @pagination)) %>
<% end %>
