<% if @users.empty? %>
  No results
<% else %>
  <%= render(PagyComponent.new(pagy: @pagy, pagy_link_proc: pagy_link_proc(@pagy))) %>

  <%=
    render(Admin::TableComponent.new(
      collection: @users,
      columns: [
        ['ID',      :id],
        ['email',   :email],
        ['PID',     proc { |item| PublicId.from_uuid(item.uuid) } ],
        ['Actions', proc { |_item| "Edit" }],
      ],
    ))
  %>

  <%= render(PagyComponent.new(pagy: @pagy, pagy_link_proc: pagy_link_proc(@pagy))) %>
<% end %>
