<%= render(PaginationComponent.new(pagination: @pagination)) %>

<% if @users.empty? %>
  No results
<% else %>
  <%=
    render(Admin::TableComponent.new(
      collection: @users,
      columns: [
        ['ID',      :id],
        ['email',   :email],
        ['SUUID',   proc { |user| user.suuid }],
        ['Actions', proc { |user| render(partial: 'edit_actions_index', locals: { user: user }) }],
      ],
    ))
  %>
<% end %>

<%= render(PaginationComponent.new(pagination: @pagination)) %>
