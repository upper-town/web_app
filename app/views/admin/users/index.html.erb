<%= render(partial: 'shared/pagination', object: PaginationPresenter.new(@pagination)) %>

<% if @users.empty? %>
  No results
<% else %>
  <%=
    # TODO: Fix this
    render(Admin::TablePresenter.new(
      collection: @users,
      columns: [
        ['ID',      :id],
        ['email',   :email],
        ['Actions', proc { |user| render(partial: 'edit_actions_index', locals: { user: user }) }],
      ],
    ))
  %>
<% end %>

<%= render(partial: 'shared/pagination', object: PaginationPresenter.new(@pagination)) %>
