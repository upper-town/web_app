<% pagination_cursor = PaginationCursorPresenter.new(@pagination_cursor) %>

<%= render(pagination_cursor) %>

<% if @server_votes.empty? %>
  No results
<% else %>
  <% @server_votes.map do |server_vote| %>
    <pre>
      <%= JSON.pretty_generate(server_vote.attributes) %>
    </pre>
    <br />
  <% end %>

  <%= render(pagination_cursor) %>
<% end %>
