<%= game_select.form.select(
  :game_id,
  (
    options_for_select(
      [game_select.blank_option],
      game_select.selected_value
    ) + options_for_select(
      game_select.options,
      game_select.selected_value
    )
  ),
  {},
  {
    class: 'form-select mb-2',
    data: { 'controller' => 'game-select' }
  }
) %>
