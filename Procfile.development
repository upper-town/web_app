web: bin/rails server --port ${APP_PORT}
worker: bundle exec sidekiq --environment ${RAILS_ENV}

js: yarn run build --watch
css: yarn run watch:css

mailcatcher: gem install mailcatcher --pre && mailcatcher --foreground --verbose
